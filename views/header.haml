#header.block-clear
  %h1.inline-block
    - if @title == Coconut::Config["wiki_name"]
      = @title
    - else
      = link_to Coconut::Config["wiki_name"], "/"
      %span(style="font-weight: normal") &raquo;
      = @title